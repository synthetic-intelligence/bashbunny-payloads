#!/bin/bash

# Configuration
#SOURCE="/dev/nandf"
#DEST="/root/udisk"
#PREFIX="nandf-backup"
#COMPRESS_LEVEL="1"
#DATE="$(date +%Y%m%d-%H%M%S)"
#OUTPUT="$DEST/${PREFIX}-${DATE}.img.gz"

dd if="$SOURCE" bs=4M | gzip -"${COMPRESS_LEVEL}" > "$OUTPUT"